# Windows 侧跨平台联调结果

> 对应 `docs/CROSS_PLATFORM_PLAN.md`。本文由 Windows 侧填写。
>
> 环境：Windows 11 Pro 26200 / 分支 `macos-platform-impl` / `cargo build --release`
> / `CLIPSYNC_LOG=debug` / 114 个单元测试全过。
>
> 对端 Mac：`unknown-host` `293c7c603c6109c7`，地址
> `192.168.2.226`、`192.168.2.225`、`100.88.88.11`、`fd7a:115c:a1e0::7801:d771`。
> 本机：`KPC` `86f3754c994e3c1d` / `192.168.2.238`、`100.88.88.55`、
> `fd7a:115c:a1e0::5b01:5eb1`。

---

## 摘要

| 项 | 结果 |
|---|---|
| 配对 | ✅ 局域网自动发现，未输 IP |
| T1 文本往返 | ✅ 发送侧确认（Mac 接收待确认） |
| T2 文件名编码 | ✅ 曾发现 NFC/NFD 差异，Mac 侧已修复，Windows 侧比对逐字节一致 |
| T3 保留字符 | ✅ 通过 |
| T4 缓存命中与续传 | ✅ 通过（限「同一发送方重复发送」；跨机独立算 id 不复用，属设计取舍，见文末） |
| T5 敏感内容 | 🔴 **发现真实泄漏缺陷，已修复并验证** |
| T6 图片 | ✅ 发送侧确认（Mac 接收待确认） |
| T7 长连接稳定性 | ✅ 2h02m：CPU 0.05%、1 次断连已自动重连；内存 +5.4MB 待观察 |

**两个重要发现**：T2 的 NFC/NFD 差异（跨平台一致性，**Mac 侧已定位根因并修复**
——见文末补充）、T5 的敏感内容泄漏（安全，Windows 侧已修复）。

---

## 配对

```
正在局域网中查找等待配对的设备…
找到设备：unknown-host（192.168.2.226）
正在连接 192.168.2.226:47685 …
✓ 配对成功：unknown-host (293c7c603c6109c7)
已记录对方 4 个可达地址
```

**未输入任何 IP**，组播发现在真实局域网跨平台工作正常。

连接建立：`05:32:30 已认证并连接对端`。方向去重按设计生效——本机 id
`86f3754c…` > Mac id `293c7c60…`，故由 Mac 主动拨号、Windows 只监听。

---

## T1 文本往返：通过（发送侧）

| 内容 | 日志 |
|---|---|
| 纯 ASCII（33 字符） | `05:34:43 已同步 [文本] 33 字节` |
| 中文+emoji+重音（`测试🎉内容 中文与emoji混排 ÀÉÎÕÜ`） | `05:34:46 已同步 [文本] 48 字节` |

本机回读 hex 验证编码无误：
`e6b58be8af95f09f8e89e58685e5aeb920e4b8ade69687e4b88e656d6f6a69e6b7b7e68e9220c380c389c38ec395c39c`
（48 字节，与日志一致；重音字母为 **NFC**，如 `À`=`c380`）。

**需 Mac 确认**：这两条是否收到且不乱码。

---

## T2 文件名编码：发现 NFC/NFD 差异 ⚠️

### Windows → Mac（我发出的源端字节）

| 文件名 | UTF-8 hex |
|---|---|
| `ascii.txt` | `61736369692e747874` |
| `café.txt` | `636166`**`c3a9`**`2e747874` ← **NFC**，`é`=U+00E9 |
| `中文文件名.txt` | `e4b8ade69687e69687e4bbb6e5908d2e747874` |
| `带 空格 的文件.txt` | `e5b8a620e7a9bae6a0bc20e79a84e69687e4bbb62e747874` |
| `测试🎉emoji.txt` | `e6b58be8af95f09f8e89656d6f6a692e747874` |

发送日志：`05:36:33 已同步 [文件] 84 字节 → 1 台设备`（5 个文件）。

**需 Mac 确认**：收到的文件名 hex，特别是 `café.txt` 是 `c3a9`(NFC) 还是
`65cc81`(NFD)。

### Mac → Windows（我实际收到的字节）

| 收到的文件名 | UTF-8 hex | 与 Windows 源端对比 |
|---|---|---|
| `ascii.txt` | `61736369692e747874` | 一致 |
| `cafe´.txt` | `636166`**`65cc81`**`2e747874` | 🔴 **NFD**，与 NFC 源端**不一致** |
| `中文文件名.txt` | `e4b8ade69687e69687e4bbb6e5908d2e747874` | 一致 |
| `带 空格 的文件.txt` | `e5b8a620e7a9bae6a0bc20e79a84e69687e4bbb62e747874` | 一致 |
| `测试🎉emoji.txt` | `e6b58be8af95f09f8e89656d6f6a692e747874` | 一致 |
| `a_b_c_d.txt` | `615f625f635f642e747874` | T3，见下 |

**结论**：中文、emoji、空格全部逐字节一致；**只有带组合重音的字符出现
NFC/NFD 差异**——Windows 源端 `é` 是单码位 `c3a9`（9 字节文件名），从 Mac
收到的是 `e`+组合重音 `65cc81`（10 字节文件名）。

### 影响评估

1. **文件可正常落盘、内容无损**，肉眼看文件名也一样，所以不影响基本可用性。
2. **但会破坏跨平台缓存命中**：缓存键 `id = hash(文件名, 大小, mtime)`，
   文件名字节不同 → id 不同 → 同一个文件在两端算出两个 id，**每次都重传**，
   断点续传也无法跨方向复用。
3. 若把收到的文件再复制回去，会产生一个"看起来同名、实为不同字节"的条目。

### 待确认

我无法确定这是 **Mac 磁盘上文件名本身就是 NFD**，还是**传输路径中被转换**。
请 Mac 侧确认其源文件 `café.txt` 在磁盘上的字节：

```bash
ls | grep caf | xxd | head -2
# 或
python3 -c "import os;[print(f.encode('utf-8').hex()) for f in os.listdir('.') if 'caf' in f]"
```

- 若 Mac 磁盘上就是 `65cc81`：说明是文件创建时的规范化，属环境差异，
  可考虑在 `meta_for_path` 里对文件名做 NFC 归一化后再算 id。
- 若 Mac 磁盘上是 `c3a9`：说明我们的读取路径做了转换，需要查
  `filelist.rs` 的 macOS 实现。

**建议的修法**（待双方确认后再动）：在 `meta_for_path` 计算 `id` 时，对文件名
先做 NFC 归一化；文件名本身仍按原样传输与落盘。这样两端算出的 id 一致，
缓存命中恢复，且不改变用户看到的文件名。代价是引入一个 Unicode 规范化依赖
（如 `unicode-normalization`）。

---

## T3 Windows 保留字符：通过 ✅

Mac 发送 `a:b?c*d.txt`（`:`、`?`、`*` 在 Windows 文件名中非法）。

**实际结果**：落地为 `a_b_c_d.txt`，内容 `reserved chars from mac` 完整，
**无报错、无崩溃**。`sanitize_name` 的输出被 NTFS 正常接受。

反向用例（Windows → Mac 的 `normal.txt`）已随 T2 一并发出。

---

## T4 缓存命中与断点续传：通过 ✅

### 缓存命中（Windows → Mac）

1. `05:42:16 已同步 [文件] 52428800 字节`（首次 50MB）
2. 插入一段文字改变 `last_hash`（否则第二次会被引擎去重，**这是测试要点**）
3. `05:42:24 已同步 [文件] 52428800 字节`（第二次同一文件）

**从 Windows 侧即可确认命中**：第二次发送后，日志中**没有出现任何
"开始发送文件"** —— 说明 Mac 完整命中缓存、根本没有索取内容。

对比首次：`05:42:16 开始发送文件 f2f1cf5da30c8aca（自偏移 0）`。

### 断点续传（Windows → Mac）

| 时刻 | 日志 |
|---|---|
| `05:46:09.093` | `已同步 [文件] 94371840 字节`（90MB） |
| `05:46:09.451` | `开始发送文件 d374775a37c9667f（自偏移 0）` |
| `05:46:10.214` | `已同步 [文本] 18 字节` ← 打断用的文字，**1.12 秒后即送达** |
| `05:46:10.243` | `剪贴板已更新，中止代际 7 的文件发送` |
| `05:48:09.725` | `开始发送文件 d374775a37c9667f（自偏移 14680064）` ← **续传** |

**同一个 file id**、**偏移 14,680,064**（14MB）→ Mac 保留了已收部分，
只索取剩余。

源文件 MD5（供 Mac 比对）：`a1abfee8e3bacbb69be992bc8a648e59`
（`t4_90mb.bin`，94,371,840 字节）。

**需 Mac 确认**：续传完成后落地文件的 MD5 是否与上值一致。

---

## T5 敏感内容：发现真实泄漏缺陷，已修复 🔴→✅

### 现象

本机未安装密码管理器，故用密码管理器实际使用的标记
（`ExcludeClipboardContentFromMonitorProcessing`）模拟。**这是真实机制，
但未在真实密码管理器下验证过**，请知悉这一局限。

首次测试：密码文本被**同步了出去**，未被拦截。

### 排查

1. 查 `sensitive.rs` 的 diff → **Windows 分支未被改动**，排除本次合并引入。
2. 隔离测试（停掉守护进程排除自身干扰）→ 标记确实没设上。
3. 让设置方进程**保持存活**再测 → `06:04:52 本地变化跳过 (Sensitive)` ✅
   检测逻辑本身正常。
4. **关键观察**：该进程 8 秒后退出，随即
   `06:05:00 已同步 [文本] 23 字节` —— **同一条密码被同步了出去**。

### 根因（与平台无关，macOS 同样受影响）

1. 程序写入"密码 + 排除标记（空数据）"→ 我们正确判定敏感并跳过
2. **但 `Skip(Sensitive)` 分支不记录内容哈希**，引擎没记住这条内容
3. 该程序退出 → 系统 flush 剪贴板 → **空数据的自定义格式被丢弃，文本仍在**
4. 剪贴板序列号变化 → 监听器重读 → 看到"无标记的密码" → 判为新内容 → **广播**

即：**任何密码管理器在清理/退出/flush 后，先前受保护的密码都可能泄漏到
所有已配对设备。**

### 修复

`SyncEngine` 增加 `sensitive_hashes`（容量 64 的有界集合）：一旦判定敏感就
按内容哈希记住，**即便标记后来消失也继续跳过**。

新增两个回归测试：
- `sensitive_content_stays_blocked_after_marker_disappears`
- `sensitive_memory_is_bounded`

### 验证

实机复现原泄漏场景（设标记 → 进程退出 flush）：

```
06:31:27 本地变化跳过 (Sensitive)   ← 标记存在
06:31:33 本地变化跳过 (Sensitive)   ← 进程退出 flush 后，仍然拦截 ✅
```

修复前此处为 `已同步 [文本] 23 字节`。**泄漏已封堵。**

> 取舍说明：该集合按内容记忆，因此若用户日后**手动**复制了完全相同的字符串，
> 也会被跳过。这是有意为之的"宁可漏同步、不可漏密码"方向；集合有 64 条上限，
> 旧条目会自然淘汰。

---

## T6 图片：通过（发送侧）

`06:34:08 已同步 [图片] 307200 字节`（320×240 位图，307200 = 320×240×4 RGBA）。

**需 Mac 确认**：能否粘贴出该图片。

Mac → Windows 方向的图片本轮未收到，请 Mac 侧补发；若 Mac 侧日志出现
`读取剪贴板图片失败: ... could not be converted`，即为计划中已记录的
16-bit 浮点 TIFF 缺口，不必当作新问题。

---

## T7 长连接稳定性：通过（有两点需持续观察）

运行时长 **2 小时 02 分**（14:34:12 → 16:36:31），期间穿插了 T1–T6 的全部测试
（含数次 50/90MB 文件传输）。

| 指标 | 基线 14:34 | 2h02m 后 16:36 | 评价 |
|---|---|---|---|
| CPU 累计 | 0.06 s | 3.78 s | ✅ 折合 **0.05%**，接近空闲 |
| 内存 | 13.3 MB | 18.7 MB | ⚠️ **+5.4 MB**，见下 |
| 断连 | — | 1 次（已自动重连） | ✅ 见下 |

### 关于那 1 次断连

```
06:31:21 已认证并连接对端
07:49:00 对端已断开
08:12:14 已认证并连接对端     ← 自动重连成功
```

**无任何错误日志**。且 Windows 侧 device_id 较大、按方向去重规则**只监听不
拨号**，故重连必然由 Mac 侧发起——23 分钟的空档正对应 Mac 侧合并 NFD 修复、
重新编译并重启守护进程的时间窗。

判断为**开发期正常重启**而非稳定性缺陷；自动重连按设计工作。若 Mac 侧日志
能确认该时段确实重启过，本项即可完全结案。

### 关于 +5.4 MB 内存增长

期间做过多次 50MB / 90MB 文件传输，分配器保留部分缓冲属正常，且增长后
稳定在 18.7 MB 未继续攀升。**但仅凭一次 2 小时观察不足以排除缓慢泄漏**，
建议后续做一次纯空闲（不传文件）的长时间观察作为对照。暂记为"待观察"，
不作为阻塞项。

---

## 需要 Mac 侧对等验证的操作

请在 Mac 上执行并把结果补进本文件：

1. **T2 反向确认（最重要）**：
   - Mac 磁盘上 `café.txt` 的文件名字节是 `c3a9` 还是 `65cc81`？
   - 从 Windows 收到的 5 个文件，各自文件名 hex 是多少？特别是 `café.txt`
     是否保持了 NFC。
   - 这决定了 NFC/NFD 差异是"环境固有"还是"我们的代码引入"。

2. **T1 接收确认**：两条文本是否收到、是否乱码。

3. **T4 接收确认**：
   - 50MB 第二次是否出现 `文件已在缓存中命中，秒同步（零传输）`
   - 90MB 续传完成后 MD5 是否为 `a1abfee8e3bacbb69be992bc8a648e59`

4. **T5 在 macOS 上的对等验证**：本次修复是平台无关的（在 `clipsync-core`），
   但请在 Mac 上确认同一泄漏路径也已封堵——用 Swift 写入
   `org.nspasteboard.ConcealedType` 标记后让进程退出，观察是否仍然拦截。

5. **T6 补发一张 Mac 截图**给 Windows。

6. **真实密码管理器验证**：我这边没装，两侧都建议用真实的 KeePassXC /
   1Password 各测一次，确认标记检测在真实场景生效。

---

## Mac 侧回复（2026-08-04）

对上述 6 项逐一回复。**第 1 项定位到根因并已修复**，第 4 项在 Mac 上复现了
泄漏、确认修复有效。

### 1. T2 NFC/NFD —— 根因是**我们的代码**，已修复 🔴→✅

你的问题问得很准，答案是**两者都不是"环境固有"**：

| 位置 | `café.txt` 的字节 | 形态 |
|---|---|---|
| Mac **磁盘上**（源文件） | `636166`**`c3a9`**`2e747874`（9 字节） | **NFC** |
| Mac 经 `NSPasteboard` **读回后** | `636166`**`65cc81`**`2e747874`（10 字节） | **NFD** |
| 你在 Windows 收到的 | `65cc81` | NFD |

**Mac 磁盘上是 NFC**（现代 APFS 保留 NFC，不再像 HFS+ 那样强制 NFD）。
转换发生在 **`NSURL` 经剪贴板往返**这一步——AppKit 的 URL 规范化会把文件名
转成 NFD。这是我在 `filelist.rs::read_file_paths` 里的疏漏，不是环境差异。

分步实测（用一个临时 example 打印每一步）：

```
1) Rust 直接读磁盘路径     "café.txt"       hex=636166c3a92e747874
2) 经 NSPasteboard 往返读回 "cafe\u{301}.txt" hex=63616665cc812e747874   ← 这里变了
3) meta_for_path 的 name   （取决于传入的是哪条路径）
```

**对 `id` 的实际影响**（你预判的完全正确）：

```
磁盘直取 : name="café.txt" id=e562a3c406cc636d
剪贴板往返: name="cafe´.txt" id=09f237c4633446cf
⚠️ id 不同 —— 同一个文件被当成两个，缓存必然不命中、断点续传失效
```

**修复**：在 `read_file_paths` 出口用 Foundation 自带的
`precomposedStringWithCanonicalMapping()` 归一化回 NFC（无需引入
`unicode-normalization` 依赖）。修复后：

```
磁盘直取 : name="café.txt" id=e562a3c406cc636d
剪贴板往返: name="café.txt" id=e562a3c406cc636d
id 相同 —— 缓存可命中
```

新增回归测试 `clipboard_roundtrip_preserves_nfc_filename`，已确认**移除归一化
后该测试会失败**（不是那种"删掉修复也照样通过"的假测试）。

> 关于你建议的"在 `meta_for_path` 里对文件名做 NFC 归一化后再算 id"：
> 我选择在**读取路径的出口**归一化，而不是在 `meta_for_path` 里。原因是
> 前者让 `read_file_paths` 返回的 `PathBuf` 从一开始就与磁盘一致，
> 下游（`id` 计算、文件读取、回环检测）全都受益；若只在算 id 时归一化，
> `ClipRead.file_paths` 里仍是 NFD 路径，将来别处用到还会再踩一次。
> 两种做法都能让 id 一致，这里只是取更靠上游的位置。

### 2. T1 接收确认 ✅

两条文本均正常收到、无乱码。整场联调中 Mac 侧共 13 次 `已应用来自 <对端> 的
剪贴板内容`。Mac 发出的两条为 18 字节（ASCII）与 28 字节（中文+emoji），
与 UTF-8 编码长度吻合。

### 3. T4 接收确认 ✅

- **缓存命中**：50MB 文件第二次出现
  `文件已在缓存中命中，秒同步（52428800 字节，零传输）` ✅
  说明 NTFS(100ns) 与 APFS(1ns) 的时间戳精度差异**没有**破坏 `id` 一致性。
- **断点续传**：从 14,680,064 字节处正确续传，
  `t4_90mb.bin` 大小 94371840，**MD5 = `a1abfee8e3bacbb69be992bc8a648e59`**
  ——与你给出的期望值一致 ✅

> 注意：这两项是在 **NFC 修复之前**测的，能通过是因为
> `xplat50.bin` / `t4_90mb.bin` 都是纯 ASCII 文件名。**含重音字符的文件在
> 修复前必然不命中**——这正是你发现的 NFD 问题的实际后果，只是当时没用
> 带重音的文件测缓存，才没暴露出来。

### 4. T5 在 macOS 上的对等验证 ✅ —— 泄漏已复现，修复有效

在 Mac 上复现你描述的路径（写入"密码 + ConcealedType 标记"，随后重写为
无标记文本，等价于 flush 后的状态）：

**带修复**：
```
07:49:01  本地变化跳过 (Sensitive)     ← 标记存在
07:49:05  本地变化跳过 (Sensitive)     ← 标记消失后仍拦截 ✅
```

**临时移除修复后**（对照验证，确认测试有效而非"怎么改都通过"）：
```
07:51:30  本地变化跳过 (Sensitive)
07:51:34  已复制 [文本] 27 字节        ← 🔴 密码进入同步流程
```

**结论：该漏洞在 macOS 上同样成立，你的修复确实封堵了它。** 这个缺陷比我
此前在 Mac 侧验证的场景深一层——我只验了"标记存在时拦截"，没想到标记会
消失。感谢揪出来。

### 5. T6 Mac 图片 ✅（已发送，待你确认）

已发送 200×150 的 **8-bit** RGBA 图片（橙色底 + 左下角黑色方块），
日志 `已同步 [图片] 120000 字节 → 1 台设备`。请确认能否粘贴、图像是否正确。

> 未用 `screencapture` 真实截图：那需要"屏幕录制"权限，未授权时报
> `could not create image from rect`。构造的 8-bit 图片走的是同一条
> 编解码路径，等价。
>
> ⚠️ 另注意 16-bit 浮点 TIFF 的已知缺口（`MACOS.md` 第 3 节）：Retina 上
> `NSImage.lockFocus` 产生的图片 `arboard` 解不了，Mac→Win 传不过去。
> 若你收不到某张 Mac 的图，先看 Mac 侧日志是不是
> `读取剪贴板图片失败: ... could not be converted`。

### 6. 真实密码管理器 —— 两侧都尚未验证 ⚠️

Mac 侧同样没装 1Password / KeePassXC，目前都是 Swift 脚本模拟
`org.nspasteboard.ConcealedType` 标记。**这一项两边都还是空白**，建议
任一侧装一个真实密码管理器补测——因为"社区约定"与"某个软件实际是否遵守"
是两回事，模拟测不出后者。

---

## 连接稳定性：一处待你确认

Mac 侧观察到一次断开与自动重连：

```
05:32:25  对端已连接: KPC
05:57:17  对端已断开: KPC          ← 断开
05:59:43  已认证并连接对端: KPC     ← 约 2.5 分钟后自动重连成功
```

断开期间 Mac 发出的一张图片被记为
`已复制 [图片] 120000 字节，但暂无对端连接`——内容未丢失也未报错，
重连后重发即正常送达。

**请确认 05:57 前后你是否重启过程序或改过配置**（你在那段时间提交了 T5
修复，很可能是重新构建运行）。若是，则属正常测试节奏、自动重连按预期工作；
若**不是**，则是非预期断连，会影响验证清单第 10 项"长时间 0 次断开"的结论，
需要进一步排查。

---

## 当前状态汇总

| 项目 | 结果 |
|---|---|
| T1 文本往返 | ✅ 双向通过，无乱码 |
| T2 文件名编码 | ✅ 已修复（NFC 归一化），根因在 Mac 侧读取路径 |
| T3 保留字符 | ✅ Windows 侧确认落地为 `a_b_c_d.txt` |
| T4 缓存命中/续传 | ✅ 双向通过，MD5 一致 |
| T5 敏感内容 | ✅ 发现并修复真实泄漏，两平台均已验证 |
| T6 图片 | ⏳ Mac→Win 待确认；已知 16-bit 浮点 TIFF 缺口 |
| T7 长连接 | ⏳ 待确认那次断连的原因 |
| 真实密码管理器 | ⚠️ 两侧均未验证 |

本次联调共发现 **2 个真实缺陷**，均已修复并配回归测试：
1. 敏感标记消失后密码泄漏（Windows 侧发现，平台无关，两平台均已验证）
2. 文件名 NFD 转换破坏跨平台缓存命中（Windows 侧发现线索，Mac 侧定位并修复）

---

## 补充：T2 的 NFD 问题已由 Mac 侧定位并修复（Windows 侧确认）

Mac 侧查明：**磁盘上的文件名本身是 NFC**，NFD 是 `NSURL` 经剪贴板往返时被
AppKit 规范化产生的。因此修复点在**读取处**（`filelist.rs` 的 macOS 分支），
用 `precomposedStringWithCanonicalMapping()` 转回 NFC。

这比我此前建议的"在 `meta_for_path` 算 id 时做归一化"更准确——它从源头消除
差异，使"直接读磁盘"与"经剪贴板读取"两条路径得到一致结果，**且不需要引入
`unicode-normalization` 依赖**。我的建议撤回。

Windows 侧确认：合并后 116 个测试全过、无编译错误。

**仍需一次跨平台复验**：Mac 侧重新发送 `café.txt`，由 Windows 侧比对收到的
文件名字节是否已为 `636166c3a92e747874`(NFC)，并确认重复发送时出现缓存命中。
本轮因 Mac 侧修复完成时连接刚重建，尚未复跑该用例。

---

## Mac 侧复验（2026-08-04 09:21）

### 1. NFC 复验 ✅ 已重新发送，待 Windows 比对

运行的是含修复的构建（二进制 15:53 编译，进程 16:12 启动）。

```
磁盘上的源文件      name_hex=636166c3a92e747874  size=16
经剪贴板读回        /tmp/mac_send/café.txt      ← 已是 NFC，不再是 cafe´
meta_for_path 产出  name_hex=636166c3a92e747874  id=e562a3c406cc636d
09:21:46            已同步 [文件] 16 字节 → 1 台设备
```

**请 Windows 侧比对**：收到的文件名字节应为 `636166c3a92e747874`（9 字节，
`c3a9`），而非此前的 `63616665cc812e747874`（10 字节，`65cc81`）。

> 说明：我在 08:14 其实已发过一次，当时连接是通的（08:12:09 完成认证），
> 日志也有 `已同步 [文件] 16 字节`。若你那边 08:14 没收到，说明还有别的
> 问题需要查；若只是没来得及看，本次 09:21 又发了一遍。

### 2. ⚠️ 更正：我此前报告的"跨平台缓存命中通过"结论不成立

**这是我上一轮报告里的一个错误，需要撤回。**

我当时看到日志有 `文件已在缓存中命中，秒同步（52428800 字节，零传输）`，
就据此认定"NTFS(100ns) 与 APFS(1ns) 时间戳精度差异没有破坏 id 一致性"。
但重新查看完整时间线后，那次命中的成因不是我说的那样：

```
05:42:08  已同步 [文件] 52428800 字节 → 1 台设备   ← Mac 发出 50MB
05:42:11  开始接收 1 个文件（共 52428800 字节）      ← Windows 回发，Mac 接收
05:42:18  已应用来自 KPC 的剪贴板内容
05:42:20  文件已在缓存中命中，秒同步（零传输）      ← 命中的是刚收下的那份
```

这是**同一个 id 在同一台机器上的二次命中**（我发出→对方回发→我这边已有
该 blob），并**没有**验证"两台机器对同一份文件独立算出相同 id"。

而这一点恰恰是存疑的。我实测了 id 的构成：

```
同名、同大小、仅 mtime 不同的两个文件：
  café.txt  name_hex=636166c3a92e747874  size=16  id=784f3b9ec8f4aea7
  café.txt  name_hex=636166c3a92e747874  size=16  id=c6322974ae7c6c52   ← id 不同
```

`meta_for_path` 的 id = hash(文件名, 大小, **mtime 纳秒**)，缓存键就是它
（`FileCache::blob_path(id)`）。所以：

**跨机器缓存命中要成立，前提是两台机器上该文件的 mtime 完全相同。**
对"我发给你、你再发回来"这种场景，接收方落地文件的 mtime 是本地写入时间，
与源端不同 → id 不同 → 不会命中。真正能命中的是"同一台机器上重复复制同一
文件"，以及"对方重发我曾收过的、id 相同的那份"。

**这不一定是缺陷**——按 (名称,大小,mtime) 派生 id 的设计本意是"文件一改动
id 就变，旧缓存自然失效"，跨机器 mtime 不同因而不复用，是保守且安全的。
但它意味着**验证清单第 7 项"重复内容秒同步"在跨平台场景下的适用范围比我
之前说的窄**，我不该把同机命中当成跨机命中的证据。

**待你确认的问题**：你那边设计时，跨机器的文件缓存复用是否本就在预期内？
- 若**不在预期内**（只求同机复用），那当前行为正确，只需在文档里写清楚
  适用范围，避免后来人误解。
- 若**在预期内**，则 id 不应含 mtime（或需改用内容哈希/(名称,大小) 组合），
  这是设计取舍，我不擅自改动。

### 3. 其余待办

- **真实密码管理器**：两侧仍均未验证，目前都是脚本模拟标记。
- **T6 图片**：Mac 侧已于 06:06 发出 8-bit 图片（`已同步 [图片] 120000 字节`），
  你的报告说本轮未收到——请确认是否与那次断连（05:57–05:59）时间重叠。

---

## Windows 侧回复（第二轮）

### 1. 你两次发的 café.txt 都收到了 —— 不用再查「别的问题」

```
08:14:22  开始接收 1 个文件（共 16 字节）→ 已接收
09:21:51  已接收 1 个文件
```

我先前看不到文件，原因是**我们自己的清理逻辑**：`finish_incoming` 调用
`cleanup_materialized_except(Some(generation))`，只保留最新一次传输的落地目录。
你 09:48 又发了 `e0.txt`–`e3.txt`，把 café 所在的旧目录删掉了。

⚠️ **这是个测试陷阱，建议双方都记住**：要核对落地文件名/内容，必须让待验文件
**成为最后一次传输**，或在收到后立即检查。否则会误判成"没收到"。

缓存 blob 里目前有**两个 16 字节的条目**——与 café.txt 的两次发送吻合，且
**两个不同的 id** 正说明文件名字节变了（NFD → NFC），与你的修复一致。但 blob
以 id 命名、不含文件名，**我无法从中读出字节**。

**请再发一次 café.txt 并且之后不要再发别的文件**，我立刻比对 hex。

### 2. 关于 id 含 mtime —— 跨机复用**本就不在设计预期内**

你的更正是对的，而且这个行为是**有意为之**，不是缺陷：

- `id` 由**发送方**计算，随 `FileMeta` 一起传给接收方；接收方的缓存**以发送方的
  id 为键**。
- 因此"同一发送方重复发同一文件"会命中（这正是 T4 验证到的、也是实用价值所在）；
- 而"两台机器各自持有同一份文件、独立算 id"不会命中——因为 mtime 本就不同。

**mtime 是刻意加入的**：它让"文件被改动 → id 变化 → 旧缓存自动失效"成立，
无需读取文件内容就能检测变更。去掉它可换来跨机复用，但会失去这个保护——
改动后的文件可能被当成缓存命中而**发送陈旧内容**，那是更严重的问题。

所以维持现状。你把它标为"保守安全的做法"是准确的。

### 3. 我此前的 T4 表述也需收窄

我写的"跨平台缓存命中通过"同样过宽。准确表述应为：
**"同一发送方重复发送同一文件时，接收方缓存命中、零传输"**——这是实测到的，
也是有价值的；但它不等于"两端对同一份文件算出相同 id"。文首摘要已相应修正。

### 4. 仍然开着的

- **真实密码管理器**：两边都还是脚本模拟，这是当前最大空白。同意你的判断。
- **T6 图片**：你 06:06 发的那张，我这边 06:00–06:35 的日志中确实没有图片接收
  记录；05:57–05:59 的断连窗口是合理解释。请在连接确认稳定后补发一次。

---

### NFC 修复 —— Windows 侧比对结果：通过 ✅

10:20:17 收到 Mac 重发的 café.txt（内容 `accent from mac`）：

| 位置 | 文件名 hex | 长度 | 形态 |
|---|---|---|---|
| Windows 源端（我最初发出的） | `636166c3a92e747874` | 9 | NFC |
| 修复前从 Mac 收到 | `63616665cc812e747874` | 10 | NFD ❌ |
| **修复后从 Mac 收到** | **`636166c3a92e747874`** | **9** | **NFC ✅** |

与 Windows 源端**逐字节一致**。`precomposedStringWithCanonicalMapping()`
的修法确认有效，T2 结案。

---

## Mac 侧终验（2026-08-04 10:20）

### 1. café.txt 已发送，**之后不再发任何文件**

按你说的"让待验文件成为最后一次传输"来做：

```
10:20:12  已同步 [文件] 16 字节 → 1 台设备
```

Mac 侧读回值（即实际上线的文件名）：

```
café.txt   hex=636166c3a92e747874   (9 字节，c3a9 = é 单码位，NFC ✅)
```

> **已由上方 Windows 侧比对确认**：10:20:17 收到，hex 与源端逐字节一致，
> T2 结案。

连接状态：自 08:12:09 建立后**未再断开**（已持续 2 小时以上），
本次发送不存在断连干扰。

### 2. 关于代际清理这个测试陷阱 —— 值得记进正式文档

你查明"看不到 café 是因为 `cleanup_materialized_except` 只保留最新代际"
这一点很有价值。它的表现是**"文件明明传成功了，去目录里却找不到"**，
极易被误判成传输失败——我上一轮就因此怀疑"是不是还有别的问题"，
差点去查一个不存在的 bug。

建议把它写进 `MACOS.md` 的调试技巧一节（我这边可以加），因为：
- 它不是缺陷，是有意的缓存管理；
- 但任何人做"核对落地文件"类验证时都会踩到；
- 规避方法很简单：让待验文件成为最后一次传输，或先复制别的内容再去查。

### 3. T6 图片：已补发 ✅

T2 结案后 `café.txt` 无需再占据最新代际，随即补发：

```
11:20:25  已同步 [图片] 120000 字节 → 1 台设备
```

200×150 **8-bit** RGBA（橙色底 + 左下角黑色方块，120000 = 200×150×4）。
连接自 08:12:09 起未断开，本次发送时已稳定运行 3 小时。

**请确认能否粘贴、图像是否正确。** 若仍收不到，那就不是断连能解释的了，
需要另查。

### 4. 结论汇总

| 项目 | 状态 |
|---|---|
| T2 NFC 修复 | ✅ **已结案**——Windows 侧比对逐字节一致 |
| T4 缓存命中 | ✅ 双方均已收窄表述为"同一发送方重发同一文件时命中" |
| id 含 mtime | ✅ 确认为设计取舍，维持现状 |
| T5 敏感内容 | ✅ 两平台均已验证修复有效（含移除修复的对照验证） |
| T6 图片 | ⏳ 11:20:25 已补发，待你确认能否粘贴 |
| 真实密码管理器 | ⚠️ 两侧均未验证 —— **当前最大空白** |
| T7 长连接 | ✅ Mac 侧自 08:12 起 2 小时 0 断开 |

本次联调共发现并修复 **2 个真实缺陷**，另**更正了 2 处过度概括的结论**
（双方各一处：我把同机命中当成跨机命中，你把 T4 表述得过宽）。
后者虽不影响代码，但会误导后来人对能力边界的判断。

---

## 内存 +5.4MB 的成因：已查明，不是泄漏

你在 T7 里标为"待观察"的内存增长，我趁 11:38 那次断连做了对照实验。

### 结论

**内存随"处理过的最大一张图片"一次性抬升，不随时间或次数累加。**
你观察到的 +5.4MB 与图片同步吻合，不是泄漏。

### 实验数据

用同一个 release 二进制、全新配置目录、无配对的纯空闲实例做对照：

| 场景 | RSS |
|---|---|
| 刚启动（纯空闲） | 10.6 MB |
| 空闲 4 分钟后 | **9.4 MB**（不升反降） |
| 读一次 200×150 图片（120 KB） | 17.1 MB（**+6.3 MB**） |
| 再读同尺寸图 ×3 | 16.8 MB（**不再增长**） |
| 读一次 2000×1500 图片（RGBA 12 MB） | 50.6 MB |
| 再读同尺寸大图 ×2 | 50.5 MB（**仍不增长**） |
| 之后只复制小段文本 | 50.8 MB（短期不回落） |
| 再过约 10 分钟 | **16.2 MB**（分配器已归还） |

### 解读

1. **不是泄漏**：重复处理同尺寸图片时 RSS 完全持平（16.8→16.8→16.8、
   50.5→50.5）。真正的泄漏会随次数线性上升。
2. **是峰值驱动的分配器保留**：一张 200×150 的小图（原始数据仅 120 KB）就
   抬升 6.3 MB，说明开销来自解码路径的中间缓冲（TIFF 解码 + RGBA 展开 +
   `image` crate 的临时分配），而非图片本身。
3. **最终会归还**：50.8 MB 在约 10 分钟后回落到 16.2 MB。所以"不归还"是我
   一开始的错误判断——只是延迟归还，被我过早下了结论。

### 对基线数字的修正

`MACOS.md` 里记的"内存 15 MB"是**纯文本同步**场景。准确说法应为：

- 纯文本/文件同步：**~10–15 MB**
- 同步过图片后：**峰值取决于图片尺寸**，之后延迟回落

我那台跑了 4 小时的联调实例稳定在 32 MB，正是因为期间同步过一张图片
（另加 9 次文件传输），与上述规律一致。

> **建议**：这不必改代码。若要压低峰值，得改 `arboard` 的读图路径（避免
> 一次性展开整张 RGBA），属于独立优化，收益有限——图片同步本就是低频操作。
> 但**文档里的内存数字应该注明适用场景**，否则后来人看到 32 MB 会误以为
> 有泄漏，就像我们这次一样。

---

## 11:38 的断连：对端进程退出，非网络问题

```
11:37:59  对端已断开: KPC
11:38:08+ TCP 连接 192.168.2.238:47684 失败: connection timed out（三个地址轮询重试）
```

诊断结果：

| 检查项 | 结果 |
|---|---|
| `ping 192.168.2.238` | ✅ 通，4.6 ms，0% 丢包 |
| `nc -z 192.168.2.238 47684` | ❌ 不通 |
| 我方网关 | ✅ 通 |
| 我方进程 | ✅ 运行 4h21m、CPU 0.1%、仍监听 47684 |

**主机在线但端口不通 ⇒ 对方的 clipsync 进程退出了**，不是网络故障。
我方重连逻辑按预期在三个地址上轮询重试，无需干预——你重新启动后会自动接上。

断开前最后的正常活动是 11:31:14 同步文本，之后到断开为止无异常。

---

# 新一轮：界面改动待验证（Mac 侧提交于 2026-08-05）

提交 `35e3202`。改了三件事，**其中第 1 件是真 bug**，且根因在跨平台代码，
Windows 侧同样中招——请优先验证。

## 1. 配对码点了看不见（bug，两平台通用）

**问题**：`pairing_cli::host()` 用 `println!` 输出配对码。从托盘启动的 GUI
程序**没有终端**，这行输出进了虚空——用户点"显示配对码"什么也看不到，
但后台其实已开始主持配对并占着 47685 端口等人连。

**修法**：新增 `crates/clipsync-app/src/dialog.rs`，用**子进程弹窗**：
- macOS：`osascript`
- Windows：`powershell` + `System.Windows.Forms.MessageBox`

没用原生 `NSAlert`/`MessageBox`，因为它们要求主线程调用，而配对跑在后台线程，
回抄到主线程改动绕；子进程方式无新依赖、两平台对称。

> ⚠️ **安全（请你也检查一下 Windows 分支）**：我最初想用字符串拼接生成
> osascript，**实测确认可被注入**——构造一个恶意设备名就能执行任意命令：
> ```
> 设备名 = 'evil" & (do shell script "echo PWNED > /tmp/pwned.txt") & "'
> → /tmp/pwned.txt 真被创建
> ```
> 设备名来自对端，属**不可信输入**。故改为用 **argv 传参**，脚本体是固定
> 字面量，用户内容永远是数据。已验证同一载荷不再生效。
>
> Windows 分支我按同样思路写了（PowerShell `param()` 接收参数），但
> **我没有 Windows 环境实测**。请你确认：
> 1. 弹窗能正常显示；
> 2. 把设备名改成含 `"` `$` `` ` `` `;` 的字符串，确认不会被 PowerShell 执行。

**弹窗时机**：放在 `TcpListener::bind()` **之后**。弹窗会阻塞该后台线程
直到用户点掉，先监听再弹才不会错过对方连接。命令行路径传
`show_dialog=false`（终端本就可见，不多弹窗打断）。

## 2. 托盘暴露设置项

原先 7 个配置项一个都没暴露到界面，只能手工改 JSON。现在菜单是：

```
ClipSync — 已连接 1 / 2 台
─────────
显示配对码…
─────────
☑ 发送图片到其它设备
☑ 发送文件到其它设备
   单次大小上限   ▸  10 MiB / 100 MiB / 500 MiB / 2 GiB / 不限制
   发送限速       ▸  不限速 / 10 / 20 / 50 MB/s
☑ 传输前自动压缩
─────────
☐ 暂停同步
☐ 开机自启
─────────
退出 ClipSync
```

**文案刻意写"发送"而非"同步"**：`allow_image`/`allow_files` 只在
`on_local_change` 检查，`on_remote` **不检查**——关掉后对方发来的图片仍会
收下。菜单如实反映行为，不制造"关了却还在收"的困惑。这是有意的取舍，
不是遗漏；若你认为应该收发都拦，那是 `clipsync-core` 的改动，需要重跑
双机验证，建议单独讨论。

**实现**：新增 `config::SettingsHandle`（`Arc<Mutex<Settings>>` + 版本计数
+ 落盘），仿照已有的 `TrayStatus`：托盘写、中枢读，每轮比对一个整数决定
是否重读。`SyncEngine` 新增 `set_limits()`/`limits()`。

**顺带修的一处**：`NetCtx` 原本在启动时把限速与压缩开关**定格**了，
托盘改了不生效——不修这个新增的菜单项就是半成品。已改为持有
`SettingsHandle` 每次读快照，限速变化时重建令牌桶。

**请验证**（Windows 的托盘实现与 macOS 共用同一份代码，但菜单渲染由系统
负责，行为可能不同）：
1. 子菜单能否正常展开；
2. 预设档是否表现为**单选**（选中一个，同组其余自动取消）；
3. 改完设置后 `settings.json` 是否立即更新；
4. 重启后设置是否保持。

## 3. 超限提示可见

`TooLarge`/`KindDisabled` 从 DEBUG 提到 INFO，并带上具体数值与调整指引：

```
INFO 内容 2000000 字节，超过单次上限 1048576 字节，未同步（可在托盘菜单调整上限）
INFO [图片] 未同步：该类型的发送已在托盘菜单中关闭
```

此前只在 DEBUG，表现为"东西没过去但哪都查不到"——**我们自己联调时就栽过
一次**（我用 400MB 文件测试，两边都没反应，查了半天才发现是超限静默跳过）。

但加了**抑制**：同一原因只在第一次打 INFO，重复时降回 DEBUG，避免关掉图片
同步后每次截图都刷一行。`Echo`/`Duplicate` 每次复制都触发，始终留在 DEBUG。

---

## Mac 侧已实测通过的项

| 项目 | 结果 |
|---|---|
| 弹窗显示 | ✅ |
| 注入防护（macOS 分支） | ✅ 恶意设备名未被执行 |
| 命令行不弹窗 | ✅ |
| `SettingsHandle` | ✅ 即时可见、正确落盘、克隆共享同一状态 |
| 超限 INFO 提示 | ✅ 含数值与指引 |
| 抑制刷屏 | ✅ 连续超限 3 次只打 1 条 |
| 类型关闭提示 | ✅ |
| 编译 / 测试 / clippy | ✅ 118 项全过，无新增警告 |

**托盘菜单的实际点击**由我这边的使用者确认过（子菜单展开、单选表现正常）。

---

## 仍然开着的（两轮未动）

**真实密码管理器验证** —— 两侧至今都是脚本模拟标记，**这是当前唯一的实质
空白**。社区约定与"某个软件实际是否遵守"是两回事，模拟测不出后者。

Windows 侧装 KeePass 免安装版即可（比 Mac 侧方便）。要验证的是：
1. 用真实密码管理器复制一个密码，确认日志出现 `本地变化跳过 (Sensitive)`；
2. 确认 Mac 侧**收不到**该内容；
3. 关掉密码管理器（触发系统 flush），确认**仍然**拦截——这正是你上轮发现的
   那条泄漏路径，值得用真实软件再走一遍。

---

## Windows 侧待办（交接给下一轮）

分支 `macos-platform-impl`，release 已构建，116 测试全过。以下三项未完成：

### 1. 弹窗目视确认（我只验了传参机制，没看过窗口）

我发现并修复了 `-Command -` 收不到位置参数的缺陷（改用环境变量），但**没有
真正弹出窗口看一眼**。请：

- 托盘点「显示配对码」，确认窗口标题、正文、配对码都正常显示
- 用含 `" $ ` ;` 的设备名再走一遍，确认既不注入也不显示错乱

> 教训：我当时只验证了「参数能否传到」，若只看命令不报错就下结论「防护通过」，
> 会漏掉传参根本不生效这件事。

### 2. 托盘子菜单与预设档

- 子菜单能否展开
- 预设档是否表现为单选
- 切换限速/压缩后是否**立即生效**（Mac 侧修了 NetCtx 启动时定格的问题，
  需实机确认）

### 3. 真实密码管理器（两轮都欠着的唯一实质空白）

装 KeePass 免安装版，三步：
1. 复制密码 → 确认日志 `跳过 (Sensitive)`
2. 确认对端**收不到**
3. **关掉密码管理器后再确认仍然拦截** ← 这正是本轮发现的泄漏路径，
   值得用真实软件而非脚本再走一遍

---

## 一条比前三个陷阱更值得记的

前面记录的三个陷阱（落地目录只留最新代际、超限静默跳过、断连窗口内不重发）
都是"行为正确但看起来像故障"。本轮双方各踩了一次**性质更严重**的：

- Mac 侧：用了 macOS 没有的 `timeout` 命令，进程压根没启动，
  输出却是「✅ 未弹窗，符合预期」
- Windows 侧：差点把「命令没报错」当成「注入防护通过」，
  而实际参数根本没传进去

共同点是**把「没有观察到失败」当成「验证通过」**。两者都会安静地给出符合
预期的假象。

**防法**：验证时主动构造一个**应该看到的正向信号**（打印实际收到的参数值、
确认进程真的启动了），而不是等着看有没有报错。

---

# 新一轮：Mac 侧缺陷排查与配对易用性改造（2026-08-07）

环境：macOS 26.5.2 / Apple Silicon / rustc 1.95.0 / 分支 `macos-platform-impl`。
起点是 118 项测试全过，结束时 **139 项全过、零编译警告**。

本轮不是按待办清单推进，而是**通读全部源码找问题**，因此下面几条此前
两侧都没记录过。

## 一、四个此前未被发现的缺陷（已修）

### 1. macOS 上设备名恒为 `unknown-host`（本文开头那个 `unknown-host` 就是它）

`main.rs` 的 `hostname_best_effort()` 只查 `COMPUTERNAME` 与 `HOSTNAME`。
Windows 上前者由系统设置，没问题；**macOS 上两个都不存在**——`HOSTNAME` 是
bash 的 shell 变量、默认不导出，zsh 根本不设它，从 launchd/Finder 启动更没有。
实测两次 `std::env::var` 都是 `NotPresent`，于是恒定退化为 `unknown-host`。

后果不只是难看：多台 Mac 配对后**彼此重名**，`list` 与托盘里分不清是哪台。
本文档开头记录的对端 Mac 名字正是 `unknown-host`，两侧都没把它当成缺陷。

改为 `device_name_best_effort()`，依次尝试：`CLIPSYNC_DEVICE_NAME`（用户显式
指定）→ `COMPUTERNAME` → `scutil --get ComputerName`（macOS 上"设置 › 通用 ›
关于本机"里的名字，可含中文）→ `hostname` 命令（去掉 `.local` 后缀）→
`HOSTNAME`。实测本机现在显示「王的MacBook Pro」。

回归测试断言 `device_name_best_effort() != "unknown-host"`，两平台都应成立。

### 2. LaunchAgent plist 未做 XML 转义（macOS）

`autostart.rs` 用 `format!` 把可执行文件路径拼进 plist。路径含 `&`（如
`~/Projects/A&B/`）会生成**语法非法的 XML**：`fs::write` 照样成功，
`is_enabled()` 也照样返回 true（它只看文件在不在），要到**下次开机**才发现
launchd 静默拒绝加载。已加 `xml_escape`。

### 3. 文件元数据与路径按文件名反查，重名时错配（两平台通用）

`arboard_backend.rs` 的读取逻辑原本先收集元数据、再按 `file_name()` 反查该
保留哪些路径。**不同目录下的同名文件**会让它失效：两条路径的文件名相同，
只要其中一个取元数据失败（文件已删/无权限），反查就会把两条都留下——于是
1 条元数据配 2 条路径。上层 `hub` 用 `zip` 配对会静默取前 1 组，结果是拿着
B 文件的标识（大小/哈希）去发送 A 文件的字节，对端**校验失败**。

改为在同一次循环里成对收集。**这条在 Windows 侧同样存在**（该文件是跨平台
代码），已随本次修复一并解决，Windows 侧无需另行改动，但值得知情。

### 4. `pending_echo` 无上限增长（跨平台核心逻辑）

`engine.rs` 的回声登记是"一写一销"，但**回声不保证兑现**：写入剪贴板后用户
立刻复制了别的内容，去抖动会把这次变化合并掉，那条哈希就再也没人消费。
`clear_pending_echo()` 虽然存在却**从未被任何地方调用**（死代码）。引擎不碰
时间（设计约束，见 `core` 的"不做 I/O"），无法按时长过期，故改为容量上限
16 + FIFO 淘汰，并删掉那个死方法。

## 二、16-bit 浮点 TIFF 图片同步失败（已修）

即 `docs/MACOS.md` 第 3 节末尾记的那个遗留缺口，本轮修掉了。

做法是**不改 arboard 的正常路径**，只在它返回错误时加一层 macOS 兜底
（`clipsync-clip/src/image_mac.rs`）：用 `NSBitmapImageRep` 取像素，让 AppKit
把它重绘到一块 8-bit RGBA 画布上，一次性归一位深、通道顺序、预乘、行距、
色彩空间。AppKit 本来就是这些图的产出方，解码不成问题。

两个实现上的坑：

- **画布不能建成非预乘**。`graphicsContextWithBitmapImageRep` 底层是
  CGBitmapContext，而 Core Graphics 只支持预乘 alpha，传
  `AlphaNonpremultiplied` 会让它返回 `nil`，且不给任何错误原因——表现为
  "画布建得出来、上下文却是空的"。改为按预乘绘制，读出时再手动去预乘。
- **尺寸不能硬编码**。Retina 屏 `backingScaleFactor = 2`，`NSImage` 的
  40×30 对应的位图是 80×60，断言要以 `pixelsWide/pixelsHigh` 为准。

实测：双实例间同步一张此类图片，153600 字节完整送达；修复前发送侧报
`读取剪贴板图片失败: ... could not be converted`。

## 三、配对易用性改造（用户反馈驱动）

原先托盘只有「显示配对码…」，**加入方只能去命令行敲 `clipsync pair <码>`**。
这是个托盘常驻的图形程序，多数用户不会开终端——等于配对只做了一半。

改动：

1. **新增 `dialog::prompt()`**：单行输入框。macOS 用 `osascript` 的
   `display dialog ... default answer`，Windows 用 WinForms 手搭（`MessageBox`
   没有输入框）。与 `show_info` 同样的注入防护：内容经 argv / 环境变量传入，
   脚本体是固定字面量。
2. **托盘新增「输入配对码…（加入对方）」**：要码 → 先试局域网自动发现 →
   找不到再问一次对方 IP（对方窗口里就列着）。同局域网的常见情形全程只需
   输一个配对码。
3. **配对结果立刻生效，不必重启**。这是配套的关键改动——原先
   `NetCtx.known` 是**启动时的快照**，从托盘配对成功后，拨号线程看不见新
   设备、入站认证也不认它，用户会看到"配对成功"然后什么都同步不了，且没有
   任何提示说该重启。改为共享的 `KnownPeers`（`Arc<Mutex<Vec<_>>>`，读时
   克隆），配对成功后同时更新设备表、地址簿、局域网信标的接纳名单，以及
   托盘上的已配对台数。

## 四、Windows 弹窗字体发虚（已修，**需 Windows 侧目视确认**）

用户反馈：「任务栏点出的配置界面，title 的字体很丝滑，但界面内文字有像素感」。

根因：`dialog.rs` 起的是 **PowerShell 子进程，而 PowerShell 默认不是 DPI
感知的**。在缩放非 100% 的屏幕上（笔记本常见 125%/150%），Windows 会把非感知
进程的窗口按低分辨率绘制再**位图拉伸**上去，文字因此发虚；而标题栏由系统
DWM 独立绘制，始终清晰——正好就是用户描述的那种割裂感。

修法（`dialog.rs` 的 `DPI_PRELUDE`）：在**创建任何窗口之前**声明 DPI 感知，
优先 PerMonitorV2（`SetProcessDpiAwarenessContext(-4)`，Win10 1703+），失败退回
`SetProcessDPIAware()`。另外输入框窗口显式设 `Segoe UI 9pt`（WinForms 默认是
Win9x 时代的 `Microsoft Sans Serif`，与现代系统格格不入）、`AutoScaleMode = Dpi`，
输入框本身用 `Consolas` 等宽字体（配对码里 `0/O`、`1/l` 更好分辨）。

**这一节我无法在 Mac 上验证，请 Windows 侧确认**：

1. 「显示配对码…」的窗口，正文字体是否已经锐利（对比修复前）；
2. 「输入配对码…」的窗口能否正常弹出、布局是否正常、回车确定 / Esc 取消是否生效；
3. 在 150% 缩放下再看一遍——这才是能区分"修好了"和"本来就没问题"的场景；
4. 用含 `"` `$` 反引号 `;` 的设备名走一遍，确认既不注入也不显示错乱。

## 五、两处健壮性加固

- **配对握手不再采信对端自报的设备 ID**（`pairing_handshake.rs`）。`DeviceId`
  就是静态公钥的指纹，本地能自己算，没有理由信别人报的。自报值与公钥不符时
  后果全是"连得上但行为诡异"的那类问题：局域网信标里的 id 是按公钥算的，与
  记录对不上会被当陌生设备丢弃（发现失效）；拨号方向由 id 字典序决定，两边
  认知不一致会双双拨号（抖动）或双双不拨（连不上）。改为按公钥派生，不一致
  时打 warn 留痕。公钥本身已由 BLAKE2s 标签认证过，据它派生的 id 因此可信。
- **`recv_body` 不再按对端声明的长度全额预分配**（`transport.rs`）。`total`
  虽已由 Noise 认证，但一个 u32 能声明 4 GB；对端实现出错时会当场把内存打爆，
  而实际数据可能一个字节都没到。改为先要 1 MiB、随实际数据增长。

## 六、测试基础设施：剪贴板测试必须串行

新增的浮点 TIFF 测试一进来，原有的两个剪贴板测试就开始随机失败——**系统
剪贴板是全局单例，而 `cargo test` 默认并行**。原有那两个测试本来也存在这个
隐患，只是碰巧没撞上。

加了 `clipboard_test_lock()`（`clipsync-clip/src/lib.rs`），所有触碰系统剪贴板的
测试都先取这把锁。锁中毒时取回内部值继续，避免一个测试失败把其余全拖成连锁
失败、掩盖真实失败点。

## 七、一条方法论教训：对照实验被自己的进程污染

值得单独记，因为它差点让我**删掉一个正确的修复**。

修完浮点 TIFF 后，我想验证"修复前确实会失败"，于是写了个小程序直接调
`arboard::get_image()` 做对照。结果它**成功**了，据此我判定"缺口在当前依赖
版本下已不存在"，把整个 `image_mac.rs`（200 行）连同接线一起删了。

删完跑测试，回归测试当场失败，报的正是 `ConversionFailure`。

真实原因：跑对照程序时，**后台还留着两个 clipsync 实例**。它们监听到剪贴板
变化，把图片同步一圈后用 arboard 的 `set_image` 写回了剪贴板——对照程序读到
的是已经被转成 8-bit 的版本，当然解得了。

两点教训：

- **做剪贴板相关的对照实验前，先确认没有本程序在跑**。这个程序的本职就是
  改写剪贴板，它在场时任何剪贴板观测都不可信。
- 那条回归测试之所以能救回来，是因为它**断言了前置条件**（先用
  `NSBitmapImageRep` 确认剪贴板里真是浮点采样，不是就跳过并说明原因）。
  没有这一步，它可能只是在验证一张普通 8-bit 图，看着是绿的却守不住任何东西
  ——和本文档上一轮记的"把没有观察到失败当成验证通过"是同一类错误的两面。

## 本轮 Mac 侧已实测通过的项

| 项目 | 结果 |
|---|---|
| 设备名 | ✅ 显示「王的MacBook Pro」，不再是 `unknown-host` |
| 双实例配对（局域网自动发现，未输 IP） | ✅ |
| 配对后连接建立 | ✅ 经 `192.168.3.3:47694` |
| 文本同步 | ✅ 34 字节，17 ms 送达 |
| 16-bit 浮点 TIFF 图片同步 | ✅ 153600 字节完整送达（修复前失败） |
| macOS 输入框弹出与取消 | ✅ 确认进程真的起来了（不是"没报错"）；取消返回 `None` |
| osascript 注入防护 | ✅ 已改为自动化测试，不再依赖人工看窗口 |
| 编译 / 测试 | ✅ 139 项全过，零警告，连跑三次无波动 |

## 交给 Windows 侧

1. **第四节的 DPI 与字体**——我改了 Windows 代码但没有 Windows 环境，全部
   未经目视验证。
2. **托盘新增的「输入配对码…」**——菜单项渲染与子进程弹窗行为需实机确认。
3. **配对后免重启生效**——建议在 Windows 上走一遍：托盘配对成功后不重启，
   直接复制一段文字看是否同步。
4. **真实密码管理器**（三轮未动，仍是唯一实质空白）。装 KeePass 免安装版：
   复制密码 → 确认日志 `跳过 (Sensitive)` → 确认对端收不到 → **关掉密码管理器
   后再确认仍然拦截**。
